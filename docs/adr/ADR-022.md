# ADR-022: Execution And Observation Are Independent Concerns

## Decision
Runners execute assets and return execution outcomes.

The `ExecutionRuntime` is responsible for combining execution outcomes with behavioral evidence into an `ExecutionSession`.

This preserves clean separation of concerns between execution details and observation pipelines.

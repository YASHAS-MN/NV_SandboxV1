# ADR-020: Runtime Execution Is Delegated

## Decision
The Runner prepares execution commands.

The `ProcessExecutor` is solely responsible for invoking the operating system.

Future execution technologies (gVisor, Firecracker, Windows Sandbox) replace the executor without modifying runners.

# ADR-013: Ordered Sensor Execution

## Decision
Sensors execute according to ascending priority.

Execution order must be deterministic across every validator.

Sensor registration order shall never affect execution order.

Priority ordering eliminates non-deterministic ordering differences which would otherwise lead to transcript mismatches and break consensus.

# ADR-027: Workspace Isolation Is Mandatory

## Decision
Execution occurs only inside a temporary workspace.

The original asset is never executed directly.

Temporary workspaces are always destroyed after execution.

Execution environments are sanitized before process creation.

# ADR-014: Sensors Are Stateless

## Decision
Sensors retain no execution history between runs. Any state required for transcript construction belongs to the TranscriptBuilder or other dedicated protocol components. A sensor instance may hold references to injected collaborators (such as the ObservationBus), but it must not accumulate behavior across executions.

This is important because we'll reuse sensor instances across multiple executions in the future.

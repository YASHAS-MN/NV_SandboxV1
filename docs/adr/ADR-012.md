# ADR-012: Sensors Observe Only

## Decision
Sensors may observe execution.

Sensors must never:
- modify execution
- terminate execution
- classify behavior

They only publish observations.

This continues our philosophy:
Runtime executes -> Sensors observe -> Bus transports -> Builder constructs -> Engines interpret.

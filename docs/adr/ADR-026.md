# ADR-026: Execution Timeout Is Mandatory

## Decision
Every execution in Nebula must have a bounded execution time.

Executions exceeding the configured timeout terminate and are reported through `ExecutionResult`.

# ADR-023: ObservationBus Is Transport Only

## Decision
- `ObservationBus` transports canonical events.
- `TranscriptBuilder` constructs transcripts.
- `ObservationBus` never owns transcript state.

This matches the separation of concerns we have established for the observation pipeline.

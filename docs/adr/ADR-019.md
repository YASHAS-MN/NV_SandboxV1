# ADR-019: Runner Selection Is Capability-Based

## Decision
The `RunnerManager` selects an execution runner solely by evaluating the asset against each runner's `supports()` capability. The runtime itself must never contain language- or format-specific selection logic.

This keeps the runtime independent of specific execution technologies.

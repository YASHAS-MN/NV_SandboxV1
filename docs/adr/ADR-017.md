# ADR-017: Workspace Lifecycle Is Isolated

## Decision
The `WorkspaceManager` is solely responsible for the lifecycle of temporary execution directories. No other runtime component may create or destroy execution workspaces directly.

This will prevent the Runner from accidentally managing filesystem state.
